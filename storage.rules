rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to read and write to all storage locations
    // This is a simplified rule for development - in production you'd want more specific rules
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
} 